# South-African-History-Extractor
SAHO Dataset Builder

Builds a structured dataset from South African History Online (biographies, articles, archive records, places, and historical events), using the site's own sitemap and embedded Schema.org JSON-LD data rather than scraping raw HTML — this is both more reliable and explicitly the access method SAHO recommends for automated tools
